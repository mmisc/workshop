# JUnit 5 platform configuration.
#
# Parallel execution would interfere with Jazzer's single-fuzz-test-
# per-run model in fuzzing mode. Keep it sequential.
junit.jupiter.execution.parallel.enabled=false
